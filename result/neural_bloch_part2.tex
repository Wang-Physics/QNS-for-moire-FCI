\part{Neural many-body Bloch wavefunctions}

\section{Motivation and scope}

The second calculation tests the neural many-body Bloch construction of
Ref.~\cite{luo} directly in the continuum model benchmarked in Part~I. The
purpose is not to fit coefficients in the one-band ED Hilbert space. Instead,
a graph network dresses continuum Bloch orbitals in coordinate space and the
resulting generalized Slater determinant is optimized against the complete
interacting continuum Hamiltonian. This can generate remote-band content even
though the fixed one-body input is the mean-field first band.

This revision follows Appendix~B, Eqs.~(16)--(23), of Ref.~\cite{luo}.
In particular, the orbital transformation is the direct complex output
\begin{equation}
 J_{\bm k i}(X)=\operatorname{MLP}([\bm k,V_i^t])\in\mathbb C,
 \qquad Q_{\bm k i}=J_{\bm k i}\varphi_{\bm k i}.
 \label{eq:scope-direct-j}
\end{equation}
There is no constant-one skip, residual parameterization, or factor $0.2$;
the implemented transformation is the direct $J_{\bm k i}$ above.
The current source additionally implements three audit corrections: full-$M$
uses an unbiased complex random initialization; the node aggregation is the
literal $\sum_jm_{ij}$ in Eq.~(19), including $j=i$; and the random auxiliary
features $h_i^0,h_{ij}^0$ are explicitly retained in the trainable parameter
tree. Earlier one-hot-$M$, no-self-message checkpoints are excluded from this
Part. Figures~\ref{fig:nbresults}, \ref{fig:nbobservables13} and \ref{fig:nbobservables23} and all result
tables below use the four controlled 4,128-walker, 120-update baselines
and the internal- and outer-$C_3$ extensions. C3-QNS was halted after 116
accepted updates when the persistent-chain energy had plateaued; each of the
three outer sectors at both fillings completed 120 attempted updates. All
quoted energies use the final-ten equilibrated persistent-chain mean, with
the corresponding RMS reported as a stability diagnostic.


\section{Configuration, fixed Bloch basis, and conventions}

A walker is a first-quantized configuration
\begin{equation}
 X=\{(\bm r_i,l_i)\}_{i=1}^{N_e},\qquad
 \bm r_i\in[0,1)^2,\quad l_i^{\rm code}\in\{0,1\}.
\end{equation}
The fractional coordinate is converted to a Cartesian coordinate on the
$3\times3$ torus. The layer input used by the network is
\begin{equation}
 s_i=2l_i^{\rm code}-1\in\{-1,+1\},
\end{equation}
which is the $l_i=\pm1$ convention of Ref.~\cite{luo}. Particle labels are
only row labels of the determinant and graph; no ordered occupation string is
used.

The nine mesh momenta and plane-wave orbitals are
\begin{equation}
 \bm k_{m_1m_2}=\frac{m_1}{3}\bm b_1+
 \frac{m_2}{3}\bm b_2,\qquad m_1,m_2=0,1,2,
\end{equation}
\begin{equation}
 \phi^0_{\bm k}(\bm r,l)=
 \sum_{\bm G}u^l_{1\bm k\bm G}
 e^{i(\bm k+\bm G)\cdot\bm r}.
 \label{eq:strictbare}
\end{equation}
The reciprocal set contains the same 61 $\bm G$ vectors per layer used for
the single-particle bands in Fig.~\ref{fig:bands}. The coefficients
$u^l_{1\bm k\bm G}$ are obtained once by diagonalizing the continuum
mean-field Hamiltonian. In JAX they are constants, and in PyTorch they are
registered buffers. They are absent from the trainable parameter tree. Thus
every run uses band 1 as its mean-field orbital source; neither ED amplitudes
nor trainable plane-wave coefficients are used.

\section{Appendix-B graph network}

\subsection{Initial physical and auxiliary features}

The three reciprocal harmonics used at each vertex are
$\bm g_a\in\{\bm b_1,\bm b_2,\bm b_1+\bm b_2\}$. The immutable node
feature is
\begin{equation}
 v_i^0=\left[
 \sin(\bm g_a\!\cdot\!\bm r_i),
 \cos(\bm g_a\!\cdot\!\bm r_i),s_i
 \right]\in\mathbb R^7 .
 \label{eq:nodefeature}
\end{equation}
For every ordered pair $(i,j)$, the immutable edge feature is
\begin{equation}
 e_{ij}^0=\left[
 \sin\!\left(\bm k\!\cdot\!(\bm r_i-\bm r_j)\right),
 \cos\!\left(\bm k\!\cdot\!(\bm r_i-\bm r_j)\right),s_i
 \right]\in\mathbb R^{19},
 \label{eq:edgefeature}
\end{equation}
where all nine mesh momenta are included. In particular, the edge layer
channel is the source layer $s_i$, exactly as in Eq.~(16) of
Ref.~\cite{luo}; no additional layer channels are appended.

The complete initial features are
\begin{equation}
 V_i^0=(v_i^0,h_i^0)\in\mathbb R^{32},\qquad
 E_{ij}^0=(e_{ij}^0,h_{ij}^0)\in\mathbb R^{32}.
\end{equation}
The code therefore uses 25 auxiliary node components and 13 auxiliary edge
components. Appendix~B explicitly calls $h_i^0$ and $h_{ij}^0$ ``randomly
initialized parameters.'' They are therefore leaves of the trainable JAX
pytree (and \texttt{nn.Parameter} objects in PyTorch), so natural-gradient
updates act on them together with the other network parameters. The present
code uses one random vector shared over particle labels and one shared over
ordered pairs; sharing preserves permutation equivariance. Each component is
initialized from $\mathcal N(0,1/32)$. The paper does not specify this
distribution or sharing rule, so both remain explicit local conventions.
There is no learned input embedding before Eq.~\eqref{eq:message}.

\subsection{Two message-passing iterations}

For iteration $a=1,2$,
\begin{align}
 Q_{ij}^{a}&=W_Q^{a}E_{ij}^{a-1},\\
 K_{ij}^{a}&=W_K^{a}E_{ij}^{a-1},\\
 m_{ij}^{a}
 &=F^{a}\!\left(\sum_n
 Q_{in}^{a}\otimes K_{nj}^{a}\right)
 \odot G^{a}(E_{ij}^{a-1}),                             \label{eq:message}\\
 h_i^{a}
 &=h_i^{a-1}+U^{a}\!\left(
 [\sum_jm_{ij}^{a},V_i^{a-1}]\right),\\
 h_{ij}^{a}
 &=h_{ij}^{a-1}+H^{a}\!\left(
 [m_{ij}^{a},E_{ij}^{a-1}]\right),\\
 V_i^{a}&=(v_i^0,h_i^{a}),\\
 E_{ij}^{a}&=(e_{ij}^0,h_{ij}^{a}).
 \label{eq:graphupdate}
\end{align}
The outer product in Eq.~\eqref{eq:message} is flattened before $F^a$.
$F^a,G^a,H^a,U^a$ are one-layer real affine maps followed by exact GELU.
The two iterations have independent parameters. No division by $N_e$, no
neighbor-count normalization, and no LayerNorm is applied. Self edges remain
available to both the edge update and node aggregation. This is the literal
$\sum_j$ printed in Luo--Fu Eq.~(19), rather than the ``all other particles''
neighborhood convention of Ref.~\cite{pescia}. The literal physical features
$v_i^0,e_{ij}^0$ are reinserted after every iteration.

\section{Backflow, orbital transformation, and determinant}

The complex backflow matrix is implemented as a real, bias-free
$32\to4$ linear map. Its first and last two outputs are the real and
imaginary parts of a two-component displacement,
\begin{equation}
 \delta\bm r_i(X)=W V_i^2,\qquad
 \widetilde{\bm r}_i=\bm r_i+\delta\bm r_i .
 \label{eq:strictbackflow}
\end{equation}
There is no $\tanh$, clipping, or external backflow scale. The fixed
mean-field coefficients are evaluated only at this transformed coordinate,
\begin{equation}
 \varphi_{\bm k i}(X)=
 \sum_{\bm G}u^{l_i}_{1\bm k\bm G}
 \exp\!\left[i(\bm k+\bm G)\cdot
 (\bm r_i+\delta\bm r_i(X))\right].
 \label{eq:strictphi}
\end{equation}
Consequently, backflow does not update $u^l_{1\bm k\bm G}$. The factors
$\exp[i(\bm k+\bm G)\cdot\delta\bm r_i]$ are generated only by changing the
coordinate argument.

The neural orbital transformation uses the Cartesian two-component mesh
momentum and the final node feature,
\begin{equation}
 [V_i^2,k_x,k_y]\in\mathbb R^{34}
 \xrightarrow{\ 34\to32\ }\mathrm{GELU}
 \xrightarrow{\ 32\to2\ }(J^{\rm R}_{\bm k i},J^{\rm I}_{\bm k i}),
\end{equation}
\begin{equation}
 J_{\bm k i}=J^{\rm R}_{\bm k i}+iJ^{\rm I}_{\bm k i},\qquad
 Q_{\bm k i}=J_{\bm k i}\varphi_{\bm k i}.
 \label{eq:strictonce}
\end{equation}
The direct complex output is not converted to $1+\alpha\Delta J$.
Equation~\eqref{eq:strictonce} is implemented once: $J$ is not included
inside $\varphi$ and then multiplied again. A unit test that sets
$J_{\bm k i}=1/2$ verifies
$\det Q=2^{-N_e}\det\varphi$ to numerical precision.

For the Luo--Fu full-$M$ state,
\begin{align}
 \Phi_{\gamma i}(X)
 &=\sum_{\bm k}M_{\gamma\bm k}Q_{\bm k i}(X),\\
 \Psi_M(X)&=\det_{\gamma i}\Phi_{\gamma i}(X),
 \qquad M\in\mathbb C^{N_e\times9}.
 \label{eq:fullM}
\end{align}
All graph, $W$, $J$, and complex $M$ parameters are trainable. Only one final
generalized Slater determinant is used. Because $M$ is trainable and dense,
the Cauchy--Binet expansion of $\det(MQ)$ generally contains several
total-momentum sectors.

The no-$M$ ablation removes the matrix rather than replacing it by another
trainable map,
\begin{equation}
 \Psi_{\Gamma}(X)=
 \det_{\alpha i}Q_{\bm k_\alpha i}(X),\qquad
 \sum_{\alpha=1}^{N_e}\bm k_\alpha=\Gamma\pmod{\bm G}.
 \label{eq:nom}
\end{equation}
For $N_e=3$, the selected fractional momenta are
$\{(2,0),(2,1),(2,2)\}/3$; for $N_e=6$ they are
$\{(1,0),(1,1),(1,2),(2,0),(2,1),(2,2)\}/3$.
They minimize the sum of bare band-1 energies subject to total momentum
$\Gamma$. Each generalized orbital retains its own single-particle momentum;
only the determinant has total momentum zero. No $C_3$ label or ED rotation
eigenvalue is supplied.

\section{Internally equivariant C3-QNS extension}
\label{sec:c3qns}

The periodic Fourier encoding in Eqs.~\eqref{eq:nodefeature} and
\eqref{eq:edgefeature} guarantees torus periodicity and primitive-translation
symmetry, as intended in Ref.~\cite{luo}. It does \emph{not} by itself impose
$C_3$ equivariance: the three node harmonics close under rotation, but a
generic learned linear vector head need not transform as a vector, and the
nine continuous edge harmonics are not a closed regular representation.
The present C3-QNS therefore keeps the Luo--Fu message-passing network but
constrains its use by a Reynolds average.

Let $g=C_{3,A}$ be the $-2\pi/3$ active rotation about the reference stacking
center $A$, with Cartesian matrix $R$. For a raw network output,
\begin{align}
 \delta\bm r_i^{C_3}(X)
 &=\frac13\sum_{p=0}^{2}R^{-p}
   \delta\bm r_i^{\rm raw}(g^pX), \label{eq:c3bf}\\
 J_{\bm k i}^{C_3}(X)
 &=\frac13\sum_{p=0}^{2}
   J_{m^p\bm k,i}^{\rm raw}(g^pX), \label{eq:c3j}
\end{align}
where $m$ is the discrete one-band momentum permutation induced by $g$.
The same network parameters are used in all three terms. These definitions
give $\delta\bm r_i^{C_3}(gX)=R\delta\bm r_i^{C_3}(X)$ and the corresponding
covariance of $J$.

A gauge correction is required after backflow. In the plane-wave convention
of Part~I, the bare first-band spinor obeys
\begin{equation}
 \phi^0_{m\bm k}\!\left(g\widetilde{\bm r},l\right)
 =z_{\bm k}e^{-i\bm a_l\cdot\widetilde{\bm r}}
  \phi^0_{\bm k}(\widetilde{\bm r},l),
 \qquad \widetilde{\bm r}=\bm r+\delta\bm r ,
 \label{eq:c3sewing}
\end{equation}
where $z_{\bm k}\in U(1)$ is extracted from the continuum eigenvectors and
$\bm a_l$ is the reciprocal-momentum shift required to return the rotated
plane-wave expansion to the code's layer-dependent Bloch gauge. In the
present convention,
\begin{equation}
 \bm a_0=\bm b_1,\qquad \bm a_1=-\bm b_2 .
\end{equation}
This is the sense in which a \emph{momentum compensation} is required. It
does not alter the mesh momentum $\bm k$, mix different $\bm k$ values, or
update the fixed coefficients $u^l_{1\bm k\bm G}$.

To see the mismatch explicitly, backflow equivariance gives
\begin{equation}
 \widetilde{\bm r}_i(gX)
 =R\bm r_i+R\delta\bm r_i(X)
 =R\widetilde{\bm r}_i(X).
\end{equation}
Substitution into Eq.~\eqref{eq:c3sewing} then gives
\begin{align}
 \phi^0_{m\bm k}\!\left(\widetilde{\bm r}_i(gX),l_i\right)
 &=z_{\bm k}
 e^{-i\bm a_{l_i}\cdot[\bm r_i+\delta\bm r_i(X)]}
 \phi^0_{\bm k}\!\left(\widetilde{\bm r}_i(X),l_i\right)\nonumber\\
 &=z_{\bm k}e^{-i\bm a_{l_i}\cdot\bm r_i}
 \underbrace{e^{-i\bm a_{l_i}\cdot\delta\bm r_i(X)}}_
 {\eta_i(X)}
 \phi^0_{\bm k}\!\left(\widetilde{\bm r}_i(X),l_i\right).
 \label{eq:c3mismatch}
\end{align}
The physical continuum $C_3$ action contains the first phase, evaluated at
the actual electron coordinate $\bm r_i$. The second factor appears only
because the fixed Bloch orbital was evaluated at the neural coordinate.

Define $h_l(\delta\bm r)=\exp(i\bm b_l\cdot\delta\bm r)$. Cancellation of the
second factor requires
\begin{equation}
 \frac{h_l(R\delta\bm r)}{h_l(\delta\bm r)}
 =e^{i\bm a_l\cdot\delta\bm r}.
 \label{eq:c3hcondition}
\end{equation}
Since
$h_l(R\delta\bm r)/h_l(\delta\bm r)
=\exp\{i[(R^{\mathsf T}-I)\bm b_l]\cdot\delta\bm r\}$,
we solve the fixed two-dimensional linear system
\begin{equation}
 (R^{\mathsf T}-I)\bm b_l=\bm a_l
\end{equation}
once and form
\begin{equation}
 \overline Q_{\bm k i}(X)=
 e^{i\bm b_{l_i}\cdot\delta\bm r_i^{C_3}(X)}
 J_{\bm k i}^{C_3}(X)
 \phi^0_{\bm k}\!\left(
 \bm r_i+\delta\bm r_i^{C_3}(X),l_i\right).
 \label{eq:c3qbar}
\end{equation}
Equations~\eqref{eq:c3mismatch}--\eqref{eq:c3qbar} imply
\begin{equation}
 \overline Q_{m\bm k,i}(gX)
 =z_{\bm k}e^{-i\bm a_{l_i}\cdot\bm r_i}
  \overline Q_{\bm k i}(X).
 \label{eq:c3qcov}
\end{equation}
For a many-body minor, the product of the physical phases is
\begin{equation}
 G_A(X)^{-1}
 =\exp\!\left[-i\sum_i\bm a_{l_i}\cdot\bm r_i\right],
\end{equation}
while the product of $z_{\bm k}$ and row-sorting parity gives the
configuration-independent sewing number $\zeta_p$ used below.

The dot products are bilinear, with no complex conjugation. Because Luo--Fu
backflow is complex, the compensation can change both phase and amplitude;
it is fixed analytically by covariance and adds no trainable parameter. In
code, $\bm b_l$ is constructed once during initialization and the column
factor is applied at \texttt{src/jax\_neural\_bloch.py:173--177}; the
independent PyTorch path is
\texttt{src/neural\_bloch.py:494--503}. Omitting it leaves an order-one,
configuration-dependent character error despite an equivariant displacement.

Let $S_0=\{\bm k_\alpha\}$ be the same lowest-energy total-$\Gamma$ subset as
the no-$M$ baseline and $S_p=m^pS_0$. Sorting each minor gives
\begin{equation}
 \zeta_p=\operatorname{sgn}(m:S_p\to S_{p+1})
 \prod_{\bm k\in S_p}z_{\bm k}.
\end{equation}
For requested character $\chi_m=\exp(2\pi i m/3)$,
\begin{equation}
 c_0=1,\qquad c_{p+1}=\frac{\chi_m}{\zeta_p}c_p,\qquad c_3=c_0 ,
\end{equation}
and the final ansatz is
\begin{equation}
 \boxed{\displaystyle
 \Psi_{C_3,m}(X)=\frac13\sum_{p=0}^{2}
 c_p\,\det\!\left[\overline Q_{\bm k i}(X)\right]_
 {\bm k\in S_p}.}
 \label{eq:c3qns}
\end{equation}
This is a constrained realization of the Luo--Fu determinant index $w$: the
three minors are one symmetry orbit with shared neural parameters and fixed
sewing weights, rather than three independently optimized determinants.
The full-$M$ matrix is absent, so total momentum remains $\Gamma$.

Only primitive translations and $C_{3,A}$ are independent generators.
The other centers satisfy $C_{3,B}=T_{\bm t_{BA}}C_{3,A}$ and
$C_{3,C}=T_{\bm t_{CA}}C_{3,A}$. Translation symmetry plus
Eq.~\eqref{eq:c3bf} therefore implies the $B/C$ relations; all three centers
remain useful regression tests. On random configurations the JAX/PyTorch
log-wavefunction mismatch is $1.7\times10^{-14}$. For the frozen $\nu=1/3$, $m=0$ checkpoint, a 512-configuration
independent test gives a maximum complete-state character residual
$7.12\times10^{-5}$ (95th percentile $1.77\times10^{-5}$), consistent with
the finite 61-plane-wave truncation. Thus the constraint is numerically
satisfied; its energetic value remains a separate question.

\section{Complete-wavefunction outer $C_3$ projection}
\label{sec:outerc3}

The second symmetry construction deliberately removes the internal Reynolds
averages in Eqs.~\eqref{eq:c3bf}--\eqref{eq:c3j}.  Its raw state is exactly the
periodic no-$M$, total-$\Gamma$ generalized determinant
\begin{equation}
 D_\theta(X)=\det[Q_{\bm k_\alpha i}(X)]_{\bm k_\alpha\in S_0},
\end{equation}
with an unrestricted Luo--Fu message-passing backflow and $J$ network.  Thus
neither an individual displacement nor an individual generalized orbital is
forced to be a $C_3$ eigenobject.

The physical continuum action includes the layer-dependent Bloch-gauge sewing.
With the code's active-rotation convention, define
\begin{equation}
 (\mathcal C^aD_\theta)(X)=G_a(X)D_\theta(C_3^aX),
\end{equation}
where $G_0=1$ and $G_a$ is the accumulated product of the physical layer-gauge
factor at each preceding point on the configuration orbit.  For
$\omega=\exp(2\pi i/3)$, the complete-state projector is
\begin{equation}
 \boxed{\begin{aligned}
 \Psi_{m,\theta}(X)&=P_mD_\theta(X)\\
 &=\frac13\sum_{a=0}^{2}\omega^{-ma}G_a(X)\\[-2pt]
 &\quad\times D_\theta(C_3^aX),\qquad m=0,1,2 .
 \end{aligned}}
 \label{eq:outerc3}
\end{equation}
The three independently optimized sectors have the same 78,100 real
parameters as no $M$; projection adds no trainable parameter.  Every raw
branch is a complete graph/determinant evaluation with shared parameters.

Crucially, all stochastic quantities use Eq.~\eqref{eq:outerc3}, not one raw
branch.  Metropolis ratios are $|\Psi_m(X')/\Psi_m(X)|^2$, every kinetic or
tunnelling local-energy term contains the complete ratio
$\Psi_m(X')/\Psi_m(X)$, and SR uses
$\mathcal O_\mu(X)=\partial_{\theta_\mu}\log\Psi_m(X)$.  Setting
\texttt{outer\_c3\_projector=False} recovers the no-$M$ wavefunction exactly.

Before production, 64 random configurations gave relative projection norms
0.5411, 0.5929 and 0.6410 for $m=0,1,2$, so no sector was annihilated.  The
maximum character errors in
$\Psi_m(C_3X)/\Psi_m(X)=\omega^m$ were
$6.13\times10^{-14}$, $6.39\times10^{-14}$ and $5.02\times10^{-14}$.
All idempotency/orthogonality residuals for $P_mP_n=\delta_{mn}P_m$ were below
$7.55\times10^{-15}$, and disabling the projector changed the no-$M$ output
by exactly zero.  These tests isolate the new outer-projection logic from the


\section{Initialization and trainable parameter count}

Every affine neural-network kernel uses JAX LeCunNormal,
$\operatorname{std}=1/\sqrt{\mathrm{fan\,in}}$, with zero biases.
The auxiliary $h^0$ convention was specified above. For full-$M$, the real and
imaginary components are independently initialized with half-scale
LeCunNormal,
\begin{equation}
 \mathbb E[(\Re M_{\gamma k})^2]
 =\mathbb E[(\Im M_{\gamma k})^2]=\frac{1}{2N_k},
 \qquad \mathbb E|M_{\gamma k}|^2=\frac1{N_k}.
\end{equation}
Thus every momentum minor is accessible at step zero and there is no preferred
$\Gamma$ subset. Ref.~\cite{luo} states global LeCunNormal network
initialization but does not separately specify $M$; the complex extension and
variance split above are therefore the present user-requested convention, not
an attributed statement from the paper.

The mean-field orbital source is always band 1. However, because $W$ and
$J$ themselves are LeCunNormal rather than forced to $W=0,J=1$, the step-zero
many-body state is not claimed to equal the undressed bare Slater determinant
pointwise. This distinction is recorded to make the initialization auditable.

\begin{table}[H]
\centering
\caption{Trainable real parameter counts of the strict one-determinant
implementation.}
\label{tab:strictparams}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{@{}cccccc@{}}
\toprule
filling & $N_e$ & full $M$ & no $M$, $\Gamma$ & internal C3 & outer $P_m$\\
\midrule
$1/3$ & 3 & 78,154 & 78,100 & 78,100 & 78,100\\
$2/3$ & 6 & 78,208 & 78,100 & 78,100 & 78,100\\
\bottomrule
\end{tabular}
}
\end{table}
The difference is exactly $2N_eN_k$ real parameters for the complex
$N_e\times9$ matrix $M$. The internal Reynolds average and the external
$P_m$ projector share the raw no-$M$ parameters among all three rotated
branches, so neither symmetry construction adds a trainable parameter.

\section{Hamiltonian and local-energy estimator}

The VMC objective uses the complete continuum Hamiltonian at
$\theta=3^\circ$, screening parameter $\epsilon=5$, and a $3\times3$ torus.
It is not the one-band or five-band projected ED Hamiltonian. The local energy
contains the kinetic term, layer-dependent moir\'e potential, spatially
varying interlayer tunnelling, and periodic Coulomb interaction,
\begin{equation}
 E_{\rm loc}(X)=\frac{(H\Psi)(X)}{\Psi(X)}
 =E_{\rm kin}+E_{\rm moir\acute e}+E_{\rm tun}+E_{\rm C}.
\end{equation}
The Coulomb sum uses reciprocal-shell cutoff 8, dielectric $\epsilon=5$, and
removes the $\bm q=0$ neutral-background component. Tunnelling is evaluated
by flipping one layer index and multiplying the exact wavefunction ratio.

Cartesian derivatives of the complete complex state use the three-point
central stencil
\begin{align}
 \partial_a\Psi/\Psi&\simeq
 \frac{\Psi(X+h\hat a)-\Psi(X-h\hat a)}{2h\Psi(X)},\\
 \partial_a^2\Psi/\Psi&\simeq
 \frac{\Psi(X+h\hat a)+\Psi(X-h\hat a)-2\Psi(X)}
 {h^2\Psi(X)} .
\end{align}
Here $h=1.5\times10^{-3}a_M$. Periodic wrapping is applied after every
coordinate displacement. A previous $h,h/2,h/4$ audit is not reused as a
validation of the new checkpoints; it must be repeated after convergence.

\subsection{Raw-coordinate and full-wavefunction Hamiltonian audit}

The production JAX path is \texttt{src/jax\_neural\_bloch.py:184--248}; the
independent PyTorch reference is
\texttt{src/continuum\_vmc.py:96--206}.  Both implementations first form
$\bm r_i$ from the input \emph{raw} \texttt{positions}.  Consequently,
\begin{align}
 V_{\rm M}(X)&=-2V\sum_{i,a}\cos(\bm g_a\cdot\bm r_i+\phi_{l_i}),\\
 V_{\rm C}(X)&=\sum_{i<j}\sum_{\bm q\ne0}v_{\bm q}
 \cos[\bm q\cdot(\bm r_i-\bm r_j)]
\end{align}
never see $\delta\bm r_i$.  For tunnelling, $t(\bm r_i)$ is evaluated at
that same raw coordinate, while its numerator is the complete
$\Psi(X_{l_i\to1-l_i})$ obtained by a new \texttt{logpsi} call.  The kinetic
ratios similarly use new complete calls,
\begin{equation}
 R_{i\mu}^{\pm}=\frac{\Psi(X_{\bm r_i\to\bm r_i\pm h\hat\mu})}
 {\Psi(X)},
\end{equation}
so each displaced call rebuilds the graph and recomputes every particle's
$\delta\bm r_j$, $J_{\bm k j}$, the $M Q$ contraction, and the determinant.
No backflow displacement is cached by the Hamiltonian.

As a final energy-zero and convention check, a fixed-state regression sets
$\delta\bm r=0$, $J=1$, and a fixed $M$ selecting a known two-orbital bare
Bloch Slater determinant.  The four local-energy components are then compared
with an independent finite-basis evaluation.  This test uses 4,096 walkers,
120 burn sweeps and 10 measurements and is never used for an optimization
update.
\begin{table}[H]
\centering
\caption{Fixed-state Hamiltonian regression in meV for the complete
many-body state.  Parentheses are Monte Carlo standard errors.}
\label{tab:fixedhamiltonian}
\begin{tabular}{@{}lrrr@{}}
\toprule
term & finite-basis & Monte Carlo & difference\\
\midrule
kinetic & 39.4207 & 39.3467(3343) & $-0.0740$\\
moir\'e & $-38.5168$ & $-38.7739(5347)$ & $-0.2571$\\
tunnelling & $-29.9435$ & $-29.6118(2984)$ & $+0.3317$\\
Coulomb & $-9.6769$ & $-10.1671(3354)$ & $-0.4902$\\
\midrule
total & $-38.7164$ & $-39.2060(3354)$ & $-0.4896$\\
\bottomrule
\end{tabular}
\end{table}
Every component and the total agree within 1.5 quoted standard errors.  The
regression therefore finds no common Hamiltonian normalization, Coulomb
background, tunnelling, or energy-zero mismatch at its present precision.

A targeted regression test in
\texttt{tests/test\_jax\_neural\_bloch.py} instruments the real
\texttt{\_encoded\_graph} function.  One local-energy call produces four
complete graph evaluations: base $X$ with shape $(B,N_e,2)$, all plus and all
minus configurations with shape $(2N_eB,N_e,2)$ each, and all one-particle
layer flips with shape $(N_eB,N_e,2)$.  It checks every displaced coordinate
and every flipped layer against an independently constructed array.  The test
passes, as does the JAX--PyTorch full-local-energy parity test to
$2\times10^{-7}$ meV absolute tolerance.  No Hamiltonian input-routing error
was found in this audit.

\section{MCMC and natural-gradient optimization}

There are 4,128 simultaneous walkers. Initial fractional coordinates are
uniform on the torus and layer indices are uniform in $\{0,1\}$. Each sweep
proposes, for every walker, one random-particle Gaussian displacement with
standard deviation 0.06 in fractional supercell coordinates, followed by one
random-particle layer flip. Both use the Metropolis ratio
$\min[1,|\Psi(X^\prime)/\Psi(X)|^2]$. The present jobs use 300 burn-in sweeps
and two sweeps per update. Acceptance for coordinate and layer moves is
recorded separately.

For real parameters $\theta_a$,
\begin{equation}
 O_a(X)=\partial_{\theta_a}\log\Psi_\theta(X),
\end{equation}
\begin{align}
 S_{ab}&=\operatorname{Re}\left[
 \langle O_a^*O_b\rangle-
 \langle O_a^*\rangle\langle O_b\rangle\right],\\
 F_a&=2\operatorname{Re}\left\langle
 (E_{\rm loc}-\langle E_{\rm loc}\rangle)O_a^*\right\rangle .
\end{align}
The update is
\begin{equation}
 (S+\lambda I)\Delta\theta=F,\qquad
 \theta\leftarrow\theta-\eta\Delta\theta,
 \label{eq:strictsr}
\end{equation}
with ordinary, unpreconditioned CG. JAX JVP/VJP operations apply the metric
matrix-free. Starting at iteration 20 and after every subsequent Krylov step,
the code explicitly recomputes the authoritative residual
\begin{equation}
 r_{\rm rel}^{\rm true}=\frac{\lVert F-(S+\lambda I)\Delta\theta\rVert_2}
 {\lVert F\rVert_2}
\end{equation}
with an additional Fisher-vector product. The solve stops immediately when
$r_{\rm rel}^{\rm true}<0.02$ or at 60 CG iterations. At the cap, an update
is accepted only if $r_{\rm rel}^{\rm true}<0.05$ and
$F^{\mathsf T}\Delta\theta>0$. Otherwise the parameter update is rejected and
the following step uses the recovery values $\lambda=0.03$ and
$\eta=10^{-3}$. After two consecutive stable recovery solves, the protocol
returns to $\lambda=0.01$ and $\eta=2\times10^{-3}$. Every trace stores the
true residual, $F^{\mathsf T}\Delta\theta$, force and direction norms,
acceptances, and whether the update was applied. This is an adaptive truncated
natural gradient, not a claim that the $10^{-3}$ linear-system tolerance has
been reached.

\begin{table}[H]
\centering
\caption{Optimization and numerical settings. Quantities not specified by
Ref.~\cite{luo} are explicitly marked as local choices.}
\label{tab:strictprotocol}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{@{}lll@{}}
\toprule
quantity & current value & provenance\\
\midrule
width / message iterations & 32 / 2, unshared & Ref.~\cite{luo}, Appendix B\\
orbital MLP hidden width & 32, GELU & Ref.~\cite{luo}, Appendix B\\
final determinants & 1 & local controlled choice\\
optimizer / implementation & natural gradient / JAX 0.11.1 & Ref.~\cite{luo}\\
learning rate & $2\times10^{-3}$ & Ref.~\cite{luo}\\
walkers per update & 4,128 & Ref.~\cite{luo}\\
affine-kernel initialization & LeCunNormal & Ref.~\cite{luo}\\
$h^0$ initialization & random, trainable & random: Ref.~\cite{luo}; law/sharing: local\\
full-$M$ initialization & complex LeCunNormal & user choice; paper unspecified\\
node aggregation & $\sum_jm_{ij}$, including self & Ref.~\cite{luo}, Eq.~(19)\\
updates in present experiment & 120 & user-specified finite run\\
paper long-run statement & 1,000 for clusters $>3\times3$ & Ref.~\cite{luo}\\
burn / sweeps per update & 300 / 2 & local choice\\
proposal scale & 0.06 fractional & local choice\\
SR damping & $10^{-2}$ & local choice\\
CG range / true-residual rules & 20--60 / 0.02 stop, 0.05 accept & user-specified\\
wavefunction / SR batch & 516 / 516 & local memory choice\\
local-energy batch & 32 & local memory choice\\
finite-difference step & $1.5\times10^{-3}a_M$ & local choice\\
random seed & 83 & matched local choice\\
precision & JAX 64 bit & local numerical choice\\
\bottomrule
\end{tabular}%
}
\end{table}

All 4,128 walkers are used in the energy, force, and metric estimate at each
update. The printed within-step SEM treats walker values as an instantaneous
ensemble diagnostic; it is not an independent uncertainty. Checkpoints
at initialization, every 10 updates, the latest completed update, and update
120 store both flattened parameters, optimizer recovery state, sampler RNG,
and walker coordinates/layers. Interrupted jobs resume this complete state;
no validation configuration ever enters an update.

\section{Implementation checks completed before production}

The current complete 51-test suite passes; selected architecture, parity, observable, and local-energy checks verify:
\begin{enumerate}
 \item particle exchange changes the determinant sign and no other factor;
 \item $J_{\bm k i}$ is applied exactly once;
 \item fixed Bloch coefficients are absent from trainable parameters;
 \item shifted-coordinate Bloch evaluation agrees with an explicit
 plane-wave calculation;
 \item PyTorch and JAX $\log\Psi$ agree at identical parameters;
 \item complete JAX and PyTorch local energies agree;
 \item no-$M$ is invariant under both primitive translations and contains no
 trainable $M$ array;
 \item the complete-state $C_3$ projector satisfies character, idempotency,
 orthogonality, nonzero-sector-norm and projector-disabled no-$M$ regression
 tests before its three-sector production run;
 \item matrix-free force and Fisher-vector products agree with an explicit
 score-Jacobian calculation;
 \item checkpoint export between JAX and PyTorch is lossless;
 \item full-$M$ is reproducibly random, complex, and not one-hot;
 \item $h_i^0,h_{ij}^0$ are nonzero trainable parameters; and
 \item a one-particle graph receives its nonzero $j=i$ message in Eq.~(19);
 \item the 1-RDM uses full replaced-configuration ratios and a fixed bare
 projector, and its bare-Slater full-basis trace is $N_e$; and
 \item kinetic displacements and tunnelling flips rebuild the complete JAX
 graph, while diagonal Hamiltonian factors use raw coordinates.
\end{enumerate}
A one-update end-to-end smoke test also passed MCMC, local-energy evaluation,
truncated SR, residual recording, parameter update, and checkpoint writing.
These checks validate implementation consistency, not physical convergence.

\section{Controlled baseline and C3-QNS experiments}

Four baseline jobs were trained from independent random walker ensembles with
the same protocol. Full $M$ has a dense trainable complex momentum matrix and
therefore mixes many-body momentum sectors; no $M$ removes this matrix and uses
a fixed orbital set with total momentum $\Gamma$. A fifth job adds internally
equivariant C3-QNS at $\nu=1/3$. It was stopped at accepted update 116 after
the requested plateau check. Table~\ref{tab:adaptive-runs} lists the result
directories; the frozen C3 checkpoint is
\texttt{jax\_neural\_bloch\_step\_0116.npz}.
Three additional $\nu=1/3$ jobs use the unrestricted no-$M$ raw network with
the complete-wavefunction projector $P_m$, $m=0,1,2$. Each completed 120
updates with 4,128 persistent walkers. The identical three-sector protocol
was subsequently run at $\nu=2/3$. Sectors are optimized independently after
the common initialization; their public ordering uses the final-ten training
mean. All frozen outer-projected checkpoints are
\texttt{jax\_neural\_bloch\_step\_0120.npz}.

\begin{table}[H]
\centering
\caption{Authoritative calculations used in this report. C3-QNS was
stopped at update 116; the four baselines and six outer sectors completed 120 updates.}
\label{tab:adaptive-runs}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{@{}llll@{}}
\toprule
$\nu$ & ansatz & $\bm K_{\rm tot}$ & result-directory suffix\\
\midrule
$1/3$ & full $M$ & mixed & \texttt{adaptive\_cg120\_20260821\_full\_m\_nu1of3}\\
$1/3$ & no $M$ & $\Gamma$ & \texttt{adaptive\_cg120\_20260821\_no\_m\_gamma\_nu1of3}\\
$1/3$ & C3-QNS & $\Gamma$, $m=0$ & \texttt{c3\_qns\_adaptive120\_nu1of3}\\
$1/3$ & outer $P_0$ & $\Gamma$, $m=0$ & \texttt{outer\_c3\_projector\_nu1of3\_120/m0}\\
$1/3$ & outer $P_1$ & $\Gamma$, $m=1$ & \texttt{outer\_c3\_projector\_nu1of3\_120/m1}\\
$1/3$ & outer $P_2$ & $\Gamma$, $m=2$ & \texttt{outer\_c3\_projector\_nu1of3\_120/m2}\\
$2/3$ & outer $P_0$ & $\Gamma$, $m=0$ & \texttt{outer\_c3\_projector\_nu2of3\_120/p0}\\
$2/3$ & outer $P_1$ & $\Gamma$, $m=1$ & \texttt{outer\_c3\_projector\_nu2of3\_120/p1}\\
$2/3$ & outer $P_2$ & $\Gamma$, $m=2$ & \texttt{outer\_c3\_projector\_nu2of3\_120/p2}\\
$2/3$ & full $M$ & mixed & \texttt{adaptive\_cg120\_20260821\_full\_m\_nu2of3}\\
$2/3$ & no $M$ & $\Gamma$ & \texttt{adaptive\_cg120\_20260821\_no\_m\_gamma\_nu2of3}\\
\bottomrule
\end{tabular}%
}
\end{table}

\subsection{Training-chain diagnostics}

Table~\ref{tab:adaptive-training} summarizes the final ten attempted updates
for each frozen trace (steps 107--116 for C3-QNS).
The mean and RMS describe the persistent training chain and are not independent
energy estimates.  A rejected update contributes its sampled local energy to
the diagnostic trace but leaves the parameters unchanged.  The two
$\nu=1/3$ jobs never activated the rejection rule.  At $\nu=2/3$, both jobs
rejected 19 of 120 attempted updates; the final full-$M$ attempt was rejected
because $r_{\rm rel}^{\rm true}=0.0512$ exceeded the 0.05 cap.  The saved
step-120 checkpoint therefore contains the parameters after the last accepted
update, not the rejected direction.

\begin{table}[H]
\centering
\caption{Final-ten training diagnostics.  Energies are meV per particle;
RMS is training-chain scatter.}
\label{tab:adaptive-training}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{@{}llrrrrrr@{}}
\toprule
$\nu$ & ansatz & $\langle E/N_e\rangle$ & min. $E/N_e$ & RMS &
$\langle r_{\rm rel}\rangle$ & $\langle N_{\rm CG}\rangle$ & reject$_{120}$\\
\midrule
$1/3$ & full $M$ & $-35.9952$ & $-36.0866$ & 0.0434 & 0.0183 & 48.0 & 0\\
$1/3$ & no $M$, $\Gamma$ & $-36.7708$ & $-36.9023$ & 0.0592 & 0.0201 & 56.7 & 0\\
$1/3$ & C3-QNS, $\Gamma$ & $-37.1749$ & $-37.2188$ & 0.0287 & 0.0163 & 37.7 & 0\\
$1/3$ & outer $P_0$ & $-37.1660$ & $-37.2401$ & 0.0532 & 0.0187 & 34.1 & 0\\
$1/3$ & outer $P_1$ & $-32.6293$ & $-32.6686$ & 0.0319 & 0.0181 & 50.5 & 0\\
$1/3$ & outer $P_2$ & $-35.9573$ & $-36.0274$ & 0.0371 & 0.0179 & 48.4 & 0\\
$2/3$ & outer $P_0$ & $-52.4662$ & $-52.5856$ & 0.0484 & 0.0745 & 50.8 & 17\\
$2/3$ & outer $P_1$ & $-51.5899$ & $-51.7154$ & 0.0805 & 0.0385 & 55.6 & 23\\
$2/3$ & outer $P_2$ & $-52.7891$ & $-52.8479$ & 0.0581 & 0.0202 & 53.4 & 8\\
$2/3$ & full $M$ & $-52.0575$ & $-52.1563$ & 0.0817 & 0.0371 & 54.5 & 19\\
$2/3$ & no $M$, $\Gamma$ & $-52.4147$ & $-52.4842$ & 0.0578 & 0.0397 & 54.9 & 19\\
\bottomrule
\end{tabular}%
}
\end{table}
The final-ten mean position/layer acceptances are 0.681/0.592,
0.669/0.618 and 0.672/0.643 for the $\nu=1/3$ full-$M$, no-$M$ and C3-QNS
states, and 0.651/0.604 and 0.660/0.632 at $\nu=2/3$. The corresponding
final-ten local-energy variances are 118.0, 104.5, 90.2, 701.0 and
$718.3~\mathrm{meV}^2$. Thus a smooth energy trace does not establish a
zero-variance eigenstate.

At $\nu=2/3$, outer $P_0,P_1,P_2$ have final-ten position/layer
acceptances 0.651/0.622, 0.663/0.641 and 0.654/0.624, with mean variances
681.3, 703.9 and $684.8~\mathrm{meV}^2$. Their cumulative rejected-update
counts are 17, 23 and 8, respectively.
For outer $P_0,P_1,P_2$, the corresponding final-ten position/layer
acceptances are 0.671/0.633, 0.695/0.584 and 0.671/0.526; the mean variances
are 77.7, 120.2 and $99.2~\mathrm{meV}^2$.  All three outer runs accepted all
120 updates.

\subsection{Training-tail comparison}

Following the optimization convention of Ref.~\cite{luo}, the public energy
comparison now uses the equilibrated persistent training ensemble.  For each
trace we average the final ten measurements and quote their RMS fluctuation;
the RMS is a correlated training-chain stability diagnostic, not an
independent standard error.  Among the three outer sectors, $P_0$ is lowest at
$\nu=1/3$, with $\langle E/N_e\rangle_{\rm tail}=-37.1660$ meV and
$\Delta E_{\rm 5b}=+0.2272$ meV.  At $\nu=2/3$, $P_2$ is lowest, with
$\langle E/N_e\rangle_{\rm tail}=-52.7891$ meV and
$\Delta E_{\rm 5b}=-0.0636$ meV.  The latter is an optimization-chain result,
not yet a multi-seed variational bound.

Figure~\ref{fig:sector-summary} retains all three $m$ sectors outside Fig.~\ref{fig:nbresults}.
Its upper row plots their final-ten energy offsets from the lowest sector; the
lower row reports rejected parameter updates directly, replacing the black
failure crosses formerly overlaid on the CG traces.
At $\nu=1/3$, all displayed runs have zero rejected updates.  At $\nu=2/3$,
full $M$, no $M$ and the selected outer $P_2$ have respectively 19, 19 and
8 rejected updates.  A rejected update leaves the parameters unchanged but
its sampled local energy remains part of the diagnostic trace.
% Preserve the established Fig. 6--7 numbering.
\setcounter{figure}{5}
\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{fig6_neural_bloch_results.pdf}
\caption{\textbf{Controlled neural-Bloch optimization, multiband energy,
band mixing and CG stability.} Top row: $\nu=1/3$; bottom row: $\nu=2/3$.
Blue, red and green denote full $M$, no $M$ and internal C3-QNS; orange denotes
the lowest final-ten outer sector, $P_0$ at $\nu=1/3$ and $P_2$ at $\nu=2/3$.
Internal C3 exists only in the upper row and stops at update 116. (a,e)
Persistent-walker training energies. (b,f) One- through five-band ED energies
and final-ten training means; shaded widths are the corresponding RMS, not
independent error bars. (c,g) Bare-band populations from the
complete-wavefunction-ratio 1-RDM, including the lowest-training outer sector
and five-band ED in gray. (d,h)
Explicit true CG residuals; horizontal lines mark the 0.02 early-stop and
0.05 cap-acceptance thresholds. Rejected-update counts are summarized
separately in the text.}
\label{fig:nbresults}
\end{figure*}

\begin{figure*}[t]
\centering
\includegraphics[width=.70\textwidth]{fig6_training_summary.pdf}
\caption{\textbf{Outer-$C_3$ sector splitting and optimizer stability.}
(a,b) Final-ten energy offsets of the externally projected $P_m$ states,
measured from the lowest sector at each filling. Error bars are RMS
fluctuations of the correlated training tail, not independent standard errors.
At $\nu=1/3$, $m=0$ is selected, while $m=1,2$ lie 4.537 and 1.209 meV per
particle higher. At $\nu=2/3$, $m=2$ is selected, with $m=0,1$ higher by
0.323 and 1.199 meV per particle. The smaller spread at $\nu=2/3$ is
compatible with, but does not establish, the expected quasi-degenerate FCI
manifold. (c,d) Cumulative rejected parameter updates for the methods retained
in Fig.~\ref{fig:nbresults}; rejection concerns an optimizer step, not a Monte
Carlo configuration.}
\label{fig:sector-summary}
\end{figure*}

\section{Post-training one- and two-body observables}

Figures~\ref{fig:nbobservables13} and \ref{fig:nbobservables23} display all
three externally projected sectors. At $\nu=1/3$, the first row is the
training-energy minimum $P_0$ and is the selected out-$C_3$ result; $P_1$ and
$P_2$ are retained only for a controlled sector comparison. At $\nu=2/3$,
$P_2$ is the corresponding minimum. The panels are regenerated from the
equilibrated final training ensembles. Each contains 4,128 configurations;
energy selection and observable estimation use the same production-chain
convention as the optimization protocol of Ref.~\cite{luo}.

\subsection{Full-ratio 1-RDM and fixed bare-band projector}

For $a=(n,\bm k)$ and $b=(m,\bm q)$, the estimator is
\begin{equation}
\begin{split}
 \rho_{ab}&=\langle c_b^\dagger c_a\rangle\\
 &=2N_e\,\mathbb E_{X\sim|\Psi|^2,i,x'}\!\left[
 (\phi_a^0(x_i))^*\phi_b^0(x')
 \left(\frac{\Psi(X_{i\to x'})}{\Psi(X)}\right)^*\right].
\end{split}
\label{eq:rdmratio}
\end{equation}
The factor two accounts for uniform auxiliary sampling of the two layers.  In
every ratio, replacing one particle triggers a complete new ansatz call: the
code rebuilds the graph and recomputes all particles' $\delta\bm r_j$,
$J_{\bm k j}$, the optional $M Q$ contraction and the determinant.  The
projector is instead the fixed bare function
\begin{equation}
 \phi^0_{n\bm k}(\bm r,l)=\sum_{\bm G}u^l_{n\bm k\bm G}
 e^{i(\bm k+\bm G)\cdot\bm r};
\end{equation}
no backflow phase is multiplied into it.  Thus neural dressing appears once,
only through the complete wavefunction ratio.

All 4,128 equilibrated training configurations and 16 auxiliary points per configuration
are used to estimate the $45\times45$ five-band-projected matrix.  The band
populations are
\begin{equation}
 W_n=\frac1{N_e}\sum_{\bm k}\rho_{n\bm k,n\bm k}.
\end{equation}
\begin{table}[H]
\centering
\caption{Five-band-projected 1-RDM results from equilibrated training samples.
Band weights are percentages; parentheses are eight-block estimates.  ED
weights use the exact five-band one-body density matrix.}
\label{tab:adaptive-rdm}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{@{}llrrrrrrr@{}}
\toprule
$\nu$ & state & trace & $W_1$ & $W_2$ & $W_3$ & $W_4$ & $W_5$ & min eig.\\
\midrule
$1/3$ & five-band ED & $3$ & 96.47 & 2.95 & 0.52 & 0.05 & 0.01 & ---\\
$1/3$ & outer $P_0$ & $2.951(104)$ & $94.77(1.29)$ & $3.39(1.15)$ & $1.68(64)$ & $-0.33(1.43)$ & $-1.12(1.09)$ & $-0.189$\\
$1/3$ & outer $P_1$ & $3.071(75)$ & $81.15(1.09)$ & $16.91(82)$ & $2.41(72)$ & $1.21(1.28)$ & $0.69(99)$ & $-0.104$\\
$1/3$ & outer $P_2$ & $2.998(117)$ & $94.25(79)$ & $5.66(1.87)$ & $0.42(1.06)$ & $-0.01(83)$ & $-0.37(55)$ & $-0.125$\\
\midrule
$2/3$ & five-band ED & $6$ & 88.95 & 7.59 & 2.53 & 0.76 & 0.16 & ---\\
$2/3$ & outer $P_0$ & $5.894(130)$ & $88.68(1.16)$ & $7.60(98)$ & $1.56(92)$ & $0.86(1.27)$ & $-0.47(88)$ & $-0.175$\\
$2/3$ & outer $P_1$ & $5.898(138)$ & $87.74(1.11)$ & $7.70(1.05)$ & $2.35(1.94)$ & $2.06(33)$ & $-1.55(92)$ & $-0.235$\\
$2/3$ & outer $P_2$ & $6.144(77)$ & $90.21(1.20)$ & $8.32(72)$ & $3.75(85)$ & $0.45(78)$ & $-0.33(95)$ & $-0.281$\\
\bottomrule
\end{tabular}%
}
\end{table}
The projected traces are compatible with $N_e$ at the stated precision, but
the matrices are only Hermitized, not projected onto the positive-semidefinite
cone.  Their minimum eigenvalues from $-0.104$ to $-0.281$ therefore forbid a
stable natural-orbital eigenspace interpretation.  The diagonal band
populations are more robust and reveal a real sector dependence.  At
$\nu=1/3$, $P_0$ and $P_2$ retain 94--95\% band-1 weight, whereas $P_1$ has
only $81.15(1.09)\%$ and transfers $16.91(82)\%$ to band 2; five-band ED has
$W_1=96.47\%$ and $W_2=2.95\%$.  At $\nu=2/3$, all three sectors are closer
to the ED distribution $(88.95,7.59,2.53,0.76,0.16)\%$; the lowest-training
$P_2$ gives $(90.21,8.32,3.75,0.45,-0.33)\%$, with the final negative entry
consistent with auxiliary-coordinate noise.

The estimator now caches $\Psi_m(X)$ once per configuration block and jointly
batches all 16 replacement coordinates.  Each replacement still rebuilds the
complete graph, all $\delta\bm r_j$, $J$, the projected three-branch
determinant and the wavefunction ratio.  A 128-configuration, four-draw probe
takes 3.0 seconds; running two 32-thread jobs concurrently completes all six
4,128-by-16 sector measurements in three batches instead of repeating the
former nested small-call path.
The focused bare-Slater regression sets $\delta\bm r=0$, $J=1$, fixes a
complex $2\times9$ matrix $M$ with orthonormal rows, and samples the resulting
single determinant.  In a complete nine-orbital test basis the exact matrix is
$\rho=M^{\mathsf T}M^*$, with trace two.  With 4,096 walkers, 100 burn sweeps
and 32 auxiliary draws, the measured trace is $2.01047\pm0.01459$, the
Frobenius error is 0.05272, the Hermiticity residual is zero, and the minimum
sampled eigenvalue is $-0.02072$.  This unit test verifies occupation,
Hermiticity, PSD within sampling noise and full-basis trace normalization.

\subsection{Full structure factor, connected part, and density order}

The structure factor is evaluated directly as a two-body observable, not
reconstructed from the 1-RDM:
\begin{align}
 S_{\rm full}(\bm q)&=\frac{\langle|\rho_{\bm q}|^2\rangle}{N_e},\\
 S_c(\bm q)&=\frac{\langle|\rho_{\bm q}|^2\rangle-
 |\langle\rho_{\bm q}\rangle|^2}{N_e},\\
 m_{\bm q}&=\frac{|\langle\rho_{\bm q}\rangle|}{N_e}.
\end{align}
The ED comparison uses $S_{\rm full}$ with the same normalization.  At
$\nu=1/3$, outer $P_0,P_1,P_2$ give maximum nonzero-momentum
$S_{\rm full}=1.5154(78),1.1285(144),1.2863(192)$, respectively, compared
with the five-band ED value 1.4381.  At $\nu=2/3$ the corresponding values are
$0.7662(85),0.6778(76),0.7124(99)$, compared with ED 0.6197.  The sector
dependence is therefore visible in both the 1-RDM and the genuine two-body
observable; it is not a plotting artifact.

For $m=0,1,2$, the largest nonzero-$q$ density amplitudes are
$0.0126(38),0.0136(30),0.0113(25)$ at $\nu=1/3$ and
$0.00755(256),0.00797(285),0.00412(200)$ at $\nu=2/3$.  These small values
show negligible static translation-breaking expectation at the sampled
reciprocal vectors; the large $S_{\rm full}$ peaks are density fluctuations.
The real-space panels in Figs.~\ref{fig:nbobservables13} and
\ref{fig:nbobservables23} include the intra-moir\'e-cell Bloch density and
should not be identified with a nonzero CDW order parameter by visual
contrast alone.

\begin{figure*}[t]
\centering
\includegraphics[width=.98\textwidth]{fig7_neural_bloch_observables_nu1of3.pdf}
\caption{\textbf{$\nu=1/3$ outer-sector observables from equilibrated
training samples.} Rows are complete-wavefunction projectors $P_0,P_1,P_2$;
the first row, $P_0$, is the selected out-$C_3$ state because it has the
lowest final-ten training energy.
Columns show bare-band-1 momentum occupation from the complete-ratio 1-RDM,
the full static structure factor with ED normalization on the complete
hexagonal BZ, and folded charge density over a two-by-two moir\'e-cell
parallelogram. Shared color limits expose the strong $P_1$ band-mixing and
structure-factor difference without rescaling individual sectors.}
\label{fig:nbobservables13}
\end{figure*}

\begin{figure*}[t]
\centering
\includegraphics[width=.98\textwidth]{fig7_neural_bloch_observables_nu2of3.pdf}
\caption{\textbf{$\nu=2/3$ outer-sector observables from equilibrated
training samples.} Rows are $P_0,P_1,P_2$ and columns use the same definitions
and global color limits as Fig.~\ref{fig:nbobservables13}.  All panels use
4,128 configurations and 16 jointly batched auxiliary replacements per
configuration.  The three sectors are shown separately because both their
band populations and two-body structure factors differ beyond a simple
identical-map interpretation.}
\label{fig:nbobservables23}
\end{figure*}

\section{Conclusion}

The implemented baseline follows the printed Luo--Fu Appendix-B structure:
fixed mean-field band-1 plane-wave coefficients, periodic node and edge
features, trainable random auxiliary features, two unshared width-32 message
iterations with the literal self-inclusive $\sum_jm_{ij}$, coordinate
backflow, direct complex $J_{\bm k i}$ applied exactly once, and one final
generalized Slater determinant. Full $M$ mixes momentum sectors, whereas no
$M$ uses a fixed total-$\Gamma$ orbital set.

C3-QNS adds an internally equivariant Reynolds-averaged backflow/orbital map,
Bloch-sewing compensation at the dressed coordinate, and a tied sum of three
$C_3$-related minors. The independent character test reaches a maximum
residual $7.12\times10^{-5}$, verifying the intended $m=0$ symmetry. It is
not an outer projection of an unrestricted determinant; it is a stronger
internal inductive bias.
The new outer construction instead leaves that raw network unrestricted and
projects the complete generalized determinant with $P_m$.  Character and
projector-algebra residuals are below $6.4\times10^{-14}$ and
$7.6\times10^{-15}$, all three projected norms are nonzero, and local energy,
Metropolis ratios and SR derivatives all use the projected state.  It adds no
trainable parameter to the 78,100-parameter no-$M$ model.

On the final-ten training tail, internal C3-QNS is lowest among the
$\nu=1/3$ curves at $-37.1749$ meV per particle, while outer $P_0$ is the
lowest of the three complete-wavefunction sectors at $-37.1660$ meV.  Both
remain about 0.22 meV above five-band ED.  At $\nu=2/3$, outer $P_2$ is the
lowest training sector at $-52.7891$ meV per particle, 0.0636 meV below the
five-band ED benchmark.  This promising crossing is a correlated,
single-seed optimization-chain result and is not promoted to an independent
variational bound.

The all-sector observables show that $m=0,1,2$ cannot be represented by one
duplicated map: at $\nu=1/3$, $P_1$ has markedly enhanced band-2 weight and a
smaller structure-factor peak, while at $\nu=2/3$ the differences are weaker
but remain resolved.  Thus the complete-state projector changes both energy
and correlations, rather than merely relabeling the same generalized
determinant.
The complete-ratio 1-RDM and direct two-body structure factors remain useful
diagnostics, but their non-PSD noise and the single-seed finite optimization
preclude a natural-orbital or topological claim. The next controlled tests are
longer, multi-seed outer-$P_0$ optimization, sector-selection replication,
and generalized-orbital-subspace equivariance without forcing
every displacement field to transform independently.
